# 🛠️ ابزارهای کمکی PythonSaveh

این پوشه شامل ابزارهای کمکی برای بهبود تجربه یادگیری در پروژه PythonSaveh است.

## 📋 فهرست ابزارها

### 1. 📊 ردیابی پیشرفت (Progress Tracker)
**فایل**: `progress_tracker.py`

ردیابی پیشرفت یادگیری کاربران و محاسبه آمار.

**ویژگی‌ها:**
- ثبت مسائل حل شده
- محاسبه درصد تکمیل
- سطح‌بندی کاربران
- پیشنهاد مسائل بعدی

**استفاده:**
```python
from tools import ProgressTracker

tracker = ProgressTracker("user_id")
tracker.mark_completed("A0001", "A_basics", 10)
report = tracker.get_progress_report()
```

### 2. 🔍 تحلیل سختی (Difficulty Analyzer)
**فایل**: `difficulty_analyzer.py`

تحلیل خودکار سطح سختی مسائل.

**ویژگی‌ها:**
- تشخیص سطح سختی (1-6)
- استخراج مفاهیم برنامه‌نویسی
- تخمین زمان حل
- تولید گزارش آماری

**استفاده:**
```python
from tools import DifficultyAnalyzer

analyzer = DifficultyAnalyzer()
problem_info = analyzer.analyze_problem_file("A_basics/A0001/A0001.py")
print(f"سختی: {problem_info.difficulty}/6")
```

### 3. ✅ بررسی پاسخ‌ها (Answer Checker)
**فایل**: `answer_checker.py`

بررسی خودکار صحت راه‌حل‌های مسائل.

**ویژگی‌ها:**
- اجرای ایمن کدها
- مقایسه خروجی با پاسخ صحیح
- تولید تست‌کیس خودکار
- گزارش‌گیری از نتایج

**استفاده:**
```python
from tools import AnswerChecker

checker = AnswerChecker()
test_cases = [{'inputs': ['5', '3'], 'expected_output': '8'}]
result = checker.check_problem_solution("problem.py", test_cases)
```

### 4. 💡 سیستم راهنمایی (Hint System)
**فایل**: `hint_system.py`

ارائه راهنمایی هوشمند برای حل مسائل.

**ویژگی‌ها:**
- راهنمایی تدریجی (3 سطح)
- راهنمایی بر اساس نوع مسئله
- راهنمایی بر اساس خطاها
- مثال‌های کدی

**استفاده:**
```python
from tools import HintSystem

hint_system = HintSystem()
hints = hint_system.get_problem_hints("A0001", "problem content", level=2)
for hint in hints:
    print(hint.text)
```

### 5. 📈 تولید گزارش (Report Generator)
**فایل**: `report_generator.py`

تولید گزارش‌های جامع از پیشرفت یادگیری.

**ویژگی‌ها:**
- گزارش روزانه، هفتگی، ماهانه
- گزارش پیشرفت و عملکرد
- نمودارهای ASCII
- پیشنهادات بهبود

**استفاده:**
```python
from tools import ReportGenerator

generator = ReportGenerator()
report = generator.generate_report('daily', 'user_id')
print(report)
```

## 🚀 استفاده از اسکریپت اصلی

**فایل**: `main.py`

اسکریپت خط فرمان برای استفاده آسان از تمام ابزارها.

### دستورات موجود:

#### 1. ردیابی پیشرفت
```bash
python tools/main.py progress --user john --problem A0001
```

#### 2. تحلیل سختی
```bash
python tools/main.py analyze --directory A_basics
```

#### 3. بررسی پاسخ‌ها
```bash
python tools/main.py check --directory A_basics --problem A0001
```

#### 4. دریافت راهنمایی
```bash
python tools/main.py hint --problem A0001 --level 2
```

#### 5. تولید گزارش
```bash
python tools/main.py report --type daily --user john
```

## 📁 ساختار فایل‌ها

```
tools/
├── __init__.py              # ماژول اصلی
├── main.py                  # اسکریپت خط فرمان
├── progress_tracker.py      # ردیابی پیشرفت
├── difficulty_analyzer.py   # تحلیل سختی
├── answer_checker.py        # بررسی پاسخ‌ها
├── hint_system.py          # سیستم راهنمایی
├── report_generator.py     # تولید گزارش
└── README.md               # این فایل
```

## 📊 فایل‌های داده

ابزارها داده‌های خود را در پوشه `data/` ذخیره می‌کنند:

```
data/
├── progress_user1.json     # پیشرفت کاربر 1
├── progress_user2.json     # پیشرفت کاربر 2
└── ...
```

## 🔧 نیازمندی‌ها

```bash
pip install matplotlib seaborn
```

## 📝 مثال‌های کاربردی

### مثال 1: ردیابی پیشرفت دانشجو
```python
# ایجاد ردیاب پیشرفت
tracker = ProgressTracker("student_123")

# علامت‌گذاری مسائل حل شده
tracker.mark_completed("A0001", "A_basics", 10)
tracker.mark_completed("A0002", "A_basics", 10)

# دریافت گزارش
report = tracker.get_progress_report()
print(f"پیشرفت: {report['completion_percentage']}%")
```

### مثال 2: تحلیل مجموعه مسائل
```python
# تحلیل تمام مسائل بخش A
analyzer = DifficultyAnalyzer()
problems = analyzer.analyze_directory("A_basics")

# تولید گزارش
report = analyzer.generate_difficulty_report(problems)
print(f"میانگین سختی: {report['average_difficulty']}")
```

### مثال 3: بررسی خودکار پاسخ‌ها
```python
# بررسی تمام مسائل یک دایرکتوری
checker = AnswerChecker()
results = checker.batch_check_directory("A_basics")

# نمایش نتایج
for result in results:
    status = "✅" if result.passed else "❌"
    print(f"{status} {result.problem_id}: {result.score} امتیاز")
```

## 🎯 کاربردهای عملی

### برای مدرسان:
- ردیابی پیشرفت دانشجویان
- تحلیل سختی مسائل
- تولید گزارش‌های آموزشی

### برای دانشجویان:
- دریافت راهنمایی هوشمند
- ردیابی پیشرفت شخصی
- دریافت پیشنهادات بهبود

### برای توسعه‌دهندگان:
- بررسی خودکار کیفیت کد
- تحلیل الگوهای یادگیری
- بهینه‌سازی محتوای آموزشی

## 🔮 توسعه‌های آینده

- [ ] پشتیبانی از زبان‌های برنامه‌نویسی دیگر
- [ ] ادغام با سیستم‌های LMS
- [ ] رابط کاربری وب
- [ ] هوش مصنوعی پیشرفته‌تر
- [ ] گزارش‌های تصویری

---

**نکته**: این ابزارها در حال توسعه هستند و ممکن است تغییرات بیشتری داشته باشند.

برای سوالات و پیشنهادات، لطفاً با تیم توسعه تماس بگیرید.