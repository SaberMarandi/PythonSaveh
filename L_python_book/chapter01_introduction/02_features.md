# 1.2 ویژگی‌های پایتون

## 🌟 ویژگی‌های کلیدی

### 1. سادگی و خوانایی
```python
# پایتون
if age >= 18:
    print("شما بالغ هستید")
else:
    print("شما نابالغ هستید")
```

### 2. تفسیری (Interpreted)
- نیازی به کامپایل ندارد
- اجرای مستقیم کد
- توسعه سریع‌تر

### 3. شی‌گرا (Object-Oriented)
- همه چیز در پایتون شیء است
- پشتیبانی کامل از OOP
- قابلیت ترکیب با سایر پارادایم‌ها

### 4. تایپ پویا (Dynamic Typing)
```python
# نوع متغیر خودکار تشخیص داده می‌شود
name = "علی"        # string
age = 25           # integer
height = 1.75      # float
is_student = True  # boolean
```

### 5. مدیریت خودکار حافظه
- Garbage Collection خودکار
- نیازی به مدیریت دستی حافظه نیست
- جلوگیری از memory leak

## 🔧 ویژگی‌های فنی

### 1. Cross-Platform
- اجرا روی Windows، Linux، macOS
- کد یکبار نوشته، همه جا اجرا

### 2. کتابخانه‌های غنی
- Standard Library گسترده
- بیش از 300,000 پکیج در PyPI
- کتابخانه برای هر کاری

### 3. قابلیت گسترش
```python
# امکان استفاده از کد C/C++
import ctypes
# یا استفاده از Cython
```

### 4. Interactive Shell
```bash
$ python
>>> print("سلام دنیا!")
سلام دنیا!
>>> 2 + 3
5
```

## 📝 ویژگی‌های سینتکس

### 1. استفاده از Indentation
```python
# بلوک‌ها با تورفتگی مشخص می‌شوند
if True:
    print("این خط تورفته است")
    if True:
        print("این خط بیشتر تورفته است")
```

### 2. عدم نیاز به نقطه‌ویرگول
```python
# نیازی به ; در انتهای خط نیست
name = "احمد"
age = 30
print(name, age)
```

### 3. Multiple Assignment
```python
# تخصیص چندگانه
a, b, c = 1, 2, 3
x = y = z = 0
```

### 4. List Comprehension
```python
# ایجاد لیست به شکل فشرده
squares = [x**2 for x in range(10)]
# معادل:
# squares = []
# for x in range(10):
#     squares.append(x**2)
```

## 🎯 فلسفه طراحی (The Zen of Python)

```python
import this
```

اصول مهم:
- **Beautiful is better than ugly** - زیبا بهتر از زشت
- **Simple is better than complex** - ساده بهتر از پیچیده  
- **Readability counts** - خوانایی مهم است
- **There should be one obvious way to do it** - باید یک راه واضح برای انجام کار باشد

## 🔄 مقایسه با سایر زبان‌ها

| ویژگی | پایتون | جاوا | C++ |
|--------|---------|------|-----|
| سادگی یادگیری | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| سرعت اجرا | ⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| خوانایی کد | ⭐⭐⭐⭐⭐ | ⭐⭐⭐ | ⭐⭐ |
| کتابخانه‌ها | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ | ⭐⭐⭐ |

## 🚀 مزایای کلیدی

### 1. سرعت توسعه بالا
- کد کمتر برای همان کارکرد
- توسعه سریع پروتوتایپ
- تست و دیباگ آسان

### 2. جامعه قوی
- مستندات عالی
- پشتیبانی فعال
- منابع یادگیری فراوان

### 3. انعطاف‌پذیری
- پارادایم‌های مختلف برنامه‌نویسی
- قابلیت تطبیق با نیازهای مختلف
- امکان ترکیب با سایر زبان‌ها

## ⚠️ محدودیت‌ها

### 1. سرعت اجرا
- کندتر از زبان‌های کامپایل شده
- مناسب نیست برای سیستم‌های real-time

### 2. مصرف حافظه
- مصرف حافظه بیشتر
- مدیریت خودکار حافظه

### 3. Mobile Development
- گزینه‌های محدود برای موبایل
- بیشتر برای سرور و دسکتاپ

---

## 💡 خلاصه
پایتون زبانی است که سادگی، قدرت و انعطاف‌پذیری را در کنار هم قرار داده و آن را به یکی از محبوب‌ترین زبان‌های برنامه‌نویسی تبدیل کرده است.