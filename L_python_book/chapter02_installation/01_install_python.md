# نصب پایتون 🐍

## چرا باید پایتون رو نصب کنیم؟

فکر کن می‌خوای یه بازی کامپیوتری بازی کنی. اول باید اون بازی رو نصب کنی، درسته؟ 🎮
پایتون هم همینطوره! باید اول نصبش کنیم تا بتونیم باهاش برنامه بنویسیم.

## 🪟 نصب روی ویندوز

### مرحله 1: دانلود پایتون
1. برو به سایت [python.org](https://python.org)
2. روی دکمه **Download Python** کلیک کن
3. فایل دانلود شده رو باز کن

### مرحله 2: نصب
1. ⚠️ **مهم**: تیک **Add Python to PATH** رو حتماً بزن!
2. روی **Install Now** کلیک کن
3. صبر کن تا نصب تموم بشه (حدود 2-3 دقیقه)
4. روی **Close** کلیک کن

### مرحله 3: بررسی نصب
1. کلیدهای `Win + R` رو با هم فشار بده
2. بنویس: `cmd` و Enter بزن
3. بنویس: `python --version`
4. اگه یه چیزی شبیه `Python 3.12.0` دیدی، یعنی موفق شدی! 🎉

## 🍎 نصب روی مک (macOS)

### مرحله 1: دانلود
1. برو به [python.org](https://python.org)
2. روی **Download Python** کلیک کن
3. فایل `.pkg` رو دانلود کن

### مرحله 2: نصب
1. فایل دانلود شده رو باز کن
2. روی **Continue** کلیک کن
3. مراحل رو دنبال کن
4. رمز کامپیوترت رو وارد کن

### مرحله 3: بررسی
1. Terminal رو باز کن (از Spotlight بگرد: Terminal)
2. بنویس: `python3 --version`
3. اگه نسخه پایتون رو دیدی، آماده‌ای! ✅

## 🐧 نصب روی لینوکس

اگه لینوکس داری، احتمالاً پایتون از قبل نصبه! 😊

بررسی کن:
```bash
python3 --version
```

اگه نصب نبود:
```bash
sudo apt update
sudo apt install python3
```

## 🎯 تست نهایی

بیا ببینیم همه چی درست کار می‌کنه:

### ویندوز:
1. `Win + R` بزن
2. بنویس: `cmd`
3. بنویس: `python`
4. باید ببینی: `>>>`

### مک/لینوکس:
1. Terminal رو باز کن
2. بنویس: `python3`
3. باید ببینی: `>>>`

حالا بنویس:
```python
print("سلام دنیا!")
```

اگه دیدی `سلام دنیا!` چاپ شد، یعنی همه چی عالیه! 🎊

## 🆘 مشکل داری؟

### پایتون رو پیدا نمی‌کنه؟
- مطمئن شو که تیک **Add to PATH** رو زدی
- کامپیوتر رو یه بار Restart کن
- دوباره امتحان کن

### نسخه قدیمی نصب شده؟
- نسخه جدید رو دانلود کن
- نسخه قدیمی رو Uninstall کن
- نسخه جدید رو نصب کن

## ✨ نکته جالب

می‌دونستی پایتون اسمش از یه سریال کمدی انگلیسی به اسم "Monty Python" گرفته شده؟ 🐍🎭
به خاطر همین توی مستندات پایتون، مثال‌های خیلی خنده‌داری می‌بینی!

## 🎮 تمرین

حالا که پایتون رو نصب کردی:
1. Terminal یا CMD رو باز کن
2. `python` یا `python3` رو بنویس
3. این کدها رو امتحان کن:

```python
print("اسم من [اسمت] است")
print(2 + 2)
print("پایتون" * 3)
```

چی دیدی؟ جالب نبود؟ 😄

## ➡️ مرحله بعد

حالا که پایتون رو نصب کردی، وقتشه با محیط‌های برنامه‌نویسی آشنا بشیم!
برو به [محیط‌های برنامه‌نویسی](02_ides.md)
