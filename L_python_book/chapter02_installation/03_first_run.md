# اولین اجرای برنامه 🎉

## آماده‌ای برای جادو؟ ✨

حالا که همه چیز رو آماده کردیم، وقتشه اولین برنامه واقعی رو بنویسیم!

## 🎯 روش 1: حالت تعاملی (Interactive Mode)

این روش مثل چت کردن با پایتونه! تو یه چیزی می‌گی، پایتون جواب میده.

### باز کردن حالت تعاملی:

**ویندوز:**
```
Win + R > cmd > python
```

**مک/لینوکس:**
```
Terminal > python3
```

باید ببینی:
```python
>>>
```

این یعنی پایتون آمادست و منتظر دستورات توئه! 🎮

### بیا امتحان کنیم:

```python
>>> print("سلام!")
سلام!

>>> 2 + 2
4

>>> "پایتون" + " " + "جالبه!"
'پایتون جالبه!'

>>> 5 * 3
15
```

### خروج از حالت تعاملی:
```python
>>> exit()
```

یا کلیدهای `Ctrl + Z` (ویندوز) یا `Ctrl + D` (مک/لینوکس)

## 📝 روش 2: نوشتن فایل Python

این روش برای برنامه‌های واقعیه!

### مرحله 1: ساخت فایل

1. یه پوشه جدید بساز (مثلاً `python_projects`)
2. VS Code یا IDLE رو باز کن
3. یه فایل جدید بساز: `hello.py`

⚠️ **نکته مهم**: فایل‌های پایتون باید با `.py` تموم بشن!

### مرحله 2: نوشتن کد

توی فایل `hello.py` بنویس:

```python
# اولین برنامه من!
print("سلام دنیا!")
print("من دارم پایتون یاد می‌گیرم!")
print("این خیلی جالبه! 🎉")
```

### مرحله 3: ذخیره کردن

- `Ctrl + S` (ویندوز/لینوکس)
- `Cmd + S` (مک)

### مرحله 4: اجرا کردن

**از VS Code:**
- کلیک راست روی فایل > `Run Python File`

**از Terminal/CMD:**
```bash
cd python_projects
python hello.py
```

**از IDLE:**
- `F5` یا `Run > Run Module`

## 🎨 بیا یه برنامه جالب‌تر بنویسیم!

فایل جدید: `my_info.py`

```python
# برنامه معرفی من

print("=" * 30)
print("🌟 معرفی من 🌟")
print("=" * 30)

name = "علی"  # اسم خودت رو بنویس
age = 14      # سن خودت رو بنویس
hobby = "بازی کامپیوتری"  # علاقه‌ات رو بنویس

print(f"اسم من: {name}")
print(f"سن من: {age} سال")
print(f"علاقه من: {hobby}")

print("=" * 30)
print("خوشحالم که پایتون یاد می‌گیرم! 🐍")
```

اجراش کن و ببین چی میشه! 🎊

## 🎮 برنامه تعاملی

حالا یه برنامه بنویسیم که با کاربر حرف بزنه!

فایل: `chat.py`

```python
# برنامه چت ساده

print("سلام! من ربات پایتون هستم 🤖")

name = input("اسمت چیه؟ ")
print(f"سلام {name}! خوشبختم!")

age = input("چند سالته؟ ")
print(f"واو! {age} سال؟ عالیه!")

color = input("رنگ مورد علاقه‌ات چیه؟ ")
print(f"{color}؟ رنگ خیلی قشنگیه! 🎨")

print(f"\nخیلی خوشحالم که باهات آشنا شدم {name}! 😊")
```

این برنامه باهات حرف میزنه! جالب نیست? 🗣️

## 🐛 خطاهای رایج

### خطا 1: فایل پیدا نمیشه
```
python: can't open file 'hello.py'
```

**راه حل**: مطمئن شو توی پوشه درست هستی!
```bash
cd python_projects
python hello.py
```

### خطا 2: نام فایل اشتباه
```
SyntaxError: invalid syntax
```

**راه حل**: مطمئن شو فایل با `.py` تموم میشه!

### خطا 3: پایتون نصب نیست
```
'python' is not recognized
```

**راه حل**: برگرد به [نصب پایتون](01_install_python.md)

## 💡 نکات مهم

### 1. کامنت‌ها (Comments)
خطوطی که با `#` شروع میشن، اجرا نمیشن. برای توضیح کد هستن:

```python
# این یه کامنته - اجرا نمیشه
print("این اجرا میشه!")  # این هم کامنته
```

### 2. فاصله‌ها مهمن!
```python
print("سلام")      # ✅ درست
    print("سلام")  # ❌ غلط - فاصله اضافی!
```

### 3. نام فایل‌ها
- ✅ `hello.py`
- ✅ `my_program.py`
- ❌ `hello.txt`
- ❌ `برنامه.py` (فارسی نزن!)

## 🎯 چالش!

حالا نوبت توئه! یه برنامه بنویس که:

1. اسم و سن کاربر رو بگیره
2. بگه چند سال دیگه 18 ساله میشه
3. یه پیام خوشحال‌کننده نشون بده

**راهنمایی:**
```python
name = input("اسمت چیه؟ ")
age = int(input("چند سالته؟ "))

# حالا محاسبه کن چند سال مونده تا 18 سالگی
# و یه پیام قشنگ نشون بده!
```

## 🎊 تبریک!

تو الان:
- ✅ اولین برنامه‌ت رو نوشتی
- ✅ یاد گرفتی چطوری کد اجرا کنی
- ✅ با ورودی و خروجی کار کردی
- ✅ یه برنامه تعاملی ساختی

تو الان یه برنامه‌نویس واقعی هستی! 🎉👨‍💻

## ➡️ مرحله بعد

حالا که بلدی برنامه بنویسی و اجرا کنی، وقتشه تمرین کنی!
برو به [تمرین‌ها](04_exercises.md)
