# 4.5 تمرین‌های عملی

## 🎯 تمرین‌های این فصل

### تمرین 1: تعریف متغیرها
```python
# متغیرهای زیر را تعریف کنید:
# نام، نام خانوادگی، سن، قد، وزن، شهر محل زندگی

# راه حل:
first_name = "احمد"
last_name = "محمدی"
age = 25
height = 1.75
weight = 70
city = "تهران"

print(f"نام کامل: {first_name} {last_name}")
print(f"سن: {age} سال")
print(f"قد: {height} متر")
print(f"وزن: {weight} کیلوگرم")
print(f"شهر: {city}")
```

### تمرین 2: محاسبات ساده
```python
# دو عدد بگیرید و جمع، تفریق، ضرب و تقسیم آن‌ها را محاسبه کنید

num1 = 15
num2 = 4

sum_result = num1 + num2
sub_result = num1 - num2
mul_result = num1 * num2
div_result = num1 / num2

print(f"{num1} + {num2} = {sum_result}")
print(f"{num1} - {num2} = {sub_result}")
print(f"{num1} × {num2} = {mul_result}")
print(f"{num1} ÷ {num2} = {div_result}")
```

### تمرین 3: جابجایی متغیرها
```python
# دو متغیر تعریف کنید و مقادیر آن‌ها را جابجا کنید

a = 10
b = 20

print(f"قبل از جابجایی: a = {a}, b = {b}")

# جابجایی
a, b = b, a

print(f"بعد از جابجایی: a = {a}, b = {b}")
```

### تمرین 4: محاسبه BMI
```python
# وزن و قد فرد را بگیرید و BMI را محاسبه کنید
# فرمول: BMI = وزن / (قد × قد)

weight = 70  # کیلوگرم
height = 1.75  # متر

bmi = weight / (height ** 2)

print(f"وزن: {weight} کیلوگرم")
print(f"قد: {height} متر")
print(f"BMI: {bmi:.2f}")

# تفسیر BMI
if bmi < 18.5:
    status = "کم وزن"
elif bmi < 25:
    status = "وزن طبیعی"
elif bmi < 30:
    status = "اضافه وزن"
else:
    status = "چاق"

print(f"وضعیت: {status}")
```

### تمرین 5: محاسبه سن دقیق
```python
# سال تولد را بگیرید و سن دقیق را محاسبه کنید

birth_year = 1995
current_year = 2024

age = current_year - birth_year

print(f"سال تولد: {birth_year}")
print(f"سال جاری: {current_year}")
print(f"سن شما: {age} سال")

# محاسبه دهه تولد
decade = (birth_year // 10) * 10
print(f"دهه تولد: دهه {decade}")
```

## 🔗 ارتباط با تمرین‌های پروژه

### مسائل مرتبط در A_basics:
- **A0001**: Hello World - اولین متغیر
- **A0002**: جمع دو عدد - کار با متغیرهای عددی
- **A0003**: محاسبه مساحت مستطیل - متغیرهای هندسی
- **A0004**: تبدیل دما - تبدیل انواع داده
- **A0005**: محاسبه میانگین - عملیات ریاضی

### نحوه حل:
```python
# A0002 - جمع دو عدد
num1 = int(input("عدد اول را وارد کنید: "))
num2 = int(input("عدد دوم را وارد کنید: "))
result = num1 + num2
print(f"جمع: {result}")
```

## 🧪 تمرین‌های چالشی

### چالش 1: محاسبه فاکتوریل (بدون حلقه)
```python
# فاکتوریل 5 را بدون استفاده از حلقه محاسبه کنید
n = 5
factorial = 1 * 2 * 3 * 4 * 5
print(f"{n}! = {factorial}")
```

### چالش 2: تبدیل ثانیه به ساعت، دقیقه، ثانیه
```python
total_seconds = 3661

hours = total_seconds // 3600
minutes = (total_seconds % 3600) // 60
seconds = total_seconds % 60

print(f"{total_seconds} ثانیه = {hours} ساعت و {minutes} دقیقه و {seconds} ثانیه")
```

### چالش 3: محاسبه مساحت و محیط اشکال مختلف
```python
import math

# مربع
side = 5
square_area = side ** 2
square_perimeter = 4 * side

# دایره
radius = 3
circle_area = math.pi * radius ** 2
circle_circumference = 2 * math.pi * radius

# مثلث قائم‌الزاویه
a, b = 3, 4
c = math.sqrt(a**2 + b**2)  # وتر
triangle_area = 0.5 * a * b
triangle_perimeter = a + b + c

print(f"مربع - مساحت: {square_area}, محیط: {square_perimeter}")
print(f"دایره - مساحت: {circle_area:.2f}, محیط: {circle_circumference:.2f}")
print(f"مثلث - مساحت: {triangle_area}, محیط: {triangle_perimeter:.2f}")
```

## 📊 پروژه عملی: ماشین حساب شخصی

```python
# ماشین حساب ساده برای محاسبات روزانه

print("=== ماشین حساب شخصی ===")

# اطلاعات کاربر
name = input("نام شما: ")
print(f"سلام {name}!")

# انتخاب عملیات
print("1. جمع")
print("2. تفریق") 
print("3. ضرب")
print("4. تقسیم")
print("5. توان")
print("6. محاسبه درصد")

choice = input("انتخاب کنید (1-6): ")

# دریافت اعداد
num1 = float(input("عدد اول: "))
num2 = float(input("عدد دوم: "))

# محاسبه
if choice == "1":
    result = num1 + num2
    operation = "+"
elif choice == "2":
    result = num1 - num2
    operation = "-"
elif choice == "3":
    result = num1 * num2
    operation = "×"
elif choice == "4":
    if num2 != 0:
        result = num1 / num2
        operation = "÷"
    else:
        result = "خطا: تقسیم بر صفر"
        operation = "÷"
elif choice == "5":
    result = num1 ** num2
    operation = "^"
elif choice == "6":
    result = (num1 * num2) / 100
    operation = "% از"

# نمایش نتیجه
if isinstance(result, (int, float)):
    print(f"\n{name} عزیز، نتیجه محاسبه:")
    print(f"{num1} {operation} {num2} = {result}")
else:
    print(f"\n{result}")
```

## 🎯 نکات مهم برای تمرین

1. **همیشه نام‌های معنادار انتخاب کنید**
2. **کدتان را تست کنید**
3. **از کامنت استفاده کنید**
4. **خطاهای احتمالی را در نظر بگیرید**
5. **کد خود را مرتب و خوانا نگه دارید**

---

## 📚 منابع بیشتر
- مسائل A0001 تا A0010 در پوشه A_basics
- مسائل A0041 تا A0050 برای تبدیلات
- فصل بعدی: عملگرها