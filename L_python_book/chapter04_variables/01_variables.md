# 4.1 مفهوم متغیر

## 🤔 متغیر چیست؟

متغیر مانند یک جعبه است که می‌توانید داده‌ای را در آن نگهداری کنید. این جعبه نامی دارد و می‌توانید محتوای آن را تغییر دهید.

```python
# تعریف یک متغیر
name = "علی"
```

در این مثال:
- `name` نام متغیر است
- `"علی"` مقدار متغیر است
- `=` عملگر تخصیص است

## 📦 نحوه کار متغیرها در پایتون

### 1. تعریف متغیر
```python
# تعریف متغیرهای مختلف
student_name = "فاطمه"
student_age = 20
student_grade = 18.5
is_passed = True
```

### 2. تغییر مقدار متغیر
```python
score = 15
print(score)  # خروجی: 15

score = 18
print(score)  # خروجی: 18
```

### 3. استفاده از متغیرها
```python
first_name = "محمد"
last_name = "احمدی"
full_name = first_name + " " + last_name
print(full_name)  # خروجی: محمد احمدی
```

## 📋 قوانین نام‌گذاری متغیرها

### ✅ قوانین اجباری
1. **شروع با حرف یا _**: نام متغیر باید با حرف یا underscore شروع شود
```python
name = "صحیح"
_age = 25      # صحیح
```

2. **عدم شروع با عدد**: نمی‌توان با عدد شروع کرد
```python
# 2name = "غلط"  # خطا!
name2 = "صحیح"
```

3. **فقط حروف، اعداد و _**: فقط این کاراکترها مجاز هستند
```python
user_name = "صحیح"
userName = "صحیح"
# user-name = "غلط"  # خطا!
```

4. **حساس به بزرگی و کوچکی حروف**
```python
Name = "علی"
name = "حسن"
# این دو متغیر متفاوت هستند!
```

### 🚫 کلمات رزرو شده
این کلمات نمی‌توانند نام متغیر باشند:
```python
# and, or, not, if, else, elif, for, while, def, class, 
# import, from, as, try, except, finally, with, lambda, 
# return, yield, break, continue, pass, True, False, None
```

## 🎨 بهترین شیوه‌های نام‌گذاری

### 1. استفاده از نام‌های معنادار
```python
# ❌ بد
a = 25
b = "احمد"

# ✅ خوب
age = 25
student_name = "احمد"
```

### 2. استفاده از snake_case
```python
# ✅ توصیه شده در پایتون
first_name = "علی"
last_name = "محمدی"
birth_year = 1995
```

### 3. استفاده از نام‌های توصیفی
```python
# ❌ نامفهوم
x = 3.14159
y = 5

# ✅ واضح
pi = 3.14159
radius = 5
area = pi * radius ** 2
```

## 🔄 تخصیص چندگانه

### 1. تخصیص همزمان
```python
# تخصیص چند متغیر همزمان
a, b, c = 1, 2, 3
print(a)  # 1
print(b)  # 2
print(c)  # 3
```

### 2. تخصیص یک مقدار به چند متغیر
```python
x = y = z = 0
print(x, y, z)  # 0 0 0
```

### 3. جابجایی متغیرها
```python
a = 10
b = 20

# جابجایی ساده در پایتون
a, b = b, a

print(a)  # 20
print(b)  # 10
```

## 🧠 حافظه و متغیرها

### مفهوم Reference
```python
# در پایتون، متغیرها reference هستند
a = [1, 2, 3]
b = a  # b به همان لیست اشاره می‌کند

b.append(4)
print(a)  # [1, 2, 3, 4]
print(b)  # [1, 2, 3, 4]
```

### بررسی آدرس حافظه
```python
a = 100
b = 100
print(id(a))  # آدرس حافظه
print(id(b))  # همان آدرس (برای اعداد کوچک)
```

## 💡 نکات مهم

### 1. متغیرهای محلی و سراسری
```python
global_var = "سراسری"

def my_function():
    local_var = "محلی"
    print(local_var)   # کار می‌کند
    print(global_var)  # کار می‌کند

my_function()
# print(local_var)  # خطا! متغیر محلی در دسترس نیست
```

### 2. استفاده از del برای حذف متغیر
```python
name = "علی"
print(name)  # علی

del name
# print(name)  # خطا! متغیر حذف شده
```

## 🔍 مثال عملی

```python
# برنامه محاسبه مساحت دایره
import math

# تعریف متغیرها
radius = 5
pi = math.pi

# محاسبه
area = pi * radius ** 2
circumference = 2 * pi * radius

# نمایش نتایج
print(f"شعاع دایره: {radius}")
print(f"مساحت دایره: {area:.2f}")
print(f"محیط دایره: {circumference:.2f}")
```

---

## 📝 خلاصه
- متغیرها برای نگهداری داده‌ها استفاده می‌شوند
- نام‌گذاری باید معنادار و طبق قوانین باشد
- پایتون تایپ متغیرها را خودکار تشخیص می‌دهد
- متغیرها در پایتون reference هستند نه مقدار