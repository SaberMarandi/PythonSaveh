# ساختار یک برنامه 🏗️

## برنامه چطوری کار می‌کنه؟

فکر کن می‌خوای یه کیک درست کنی 🎂
چه کارهایی باید انجام بدی؟

1. **مواد اولیه** رو آماده کن (آرد، شکر، تخم‌مرغ)
2. **مراحل** رو دنبال کن (مخلوط کن، بپز)
3. **نتیجه** رو ببین (کیک آماده!)

برنامه‌نویسی هم دقیقاً همینطوره! 🎯

## 🎨 ساختار اصلی

هر برنامه پایتون از این قسمت‌ها تشکیل شده:

### 1. کامنت‌ها (توضیحات)
```python
# این یه کامنته
# برای توضیح کد استفاده میشه
# پایتون اینا رو اجرا نمی‌کنه
```

### 2. ورودی (Input)
```python
name = input("اسمت چیه؟ ")
```

### 3. پردازش (Processing)
```python
greeting = "سلام " + name + "!"
```

### 4. خروجی (Output)
```python
print(greeting)
```

## 📝 مثال کامل

بیا یه برنامه کامل بنویسیم:

```python
# برنامه محاسبه سن در سال 2030
# نوشته: [اسم تو]

# 1. ورودی
print("=== محاسبه سن در سال 2030 ===")
name = input("اسمت چیه؟ ")
age = int(input("چند سالته؟ "))

# 2. پردازش
years_until_2030 = 2030 - 2024
future_age = age + years_until_2030

# 3. خروجی
print(f"\n{name} عزیز!")
print(f"تو الان {age} سالته")
print(f"در سال 2030، {future_age} ساله میشی! 🎉")
```

## 🎯 اجزای مهم برنامه

### 1. عنوان و توضیحات
همیشه اول برنامه بنویس چیکار می‌کنه:

```python
# برنامه: ماشین حساب ساده
# کار: جمع دو عدد
# نویسنده: علی
# تاریخ: 1403/08/20
```

### 2. ورودی‌ها
چیزهایی که از کاربر می‌گیریم:

```python
# دریافت اطلاعات
num1 = int(input("عدد اول: "))
num2 = int(input("عدد دوم: "))
```

### 3. محاسبات
کارهایی که روی داده‌ها انجام میدیم:

```python
# محاسبه
result = num1 + num2
```

### 4. نمایش نتیجه
نشون دادن جواب به کاربر:

```python
# نمایش نتیجه
print(f"جواب: {result}")
```

## 🎮 مثال: برنامه تبدیل دما

```python
# تبدیل سانتیگراد به فارنهایت
# فرمول: F = (C × 9/5) + 32

print("🌡️ تبدیل دما 🌡️")
print("-" * 30)

# ورودی
celsius = float(input("دما به سانتیگراد: "))

# پردازش
fahrenheit = (celsius * 9/5) + 32

# خروجی
print(f"\n{celsius}°C = {fahrenheit}°F")

# پیام اضافی
if fahrenheit > 100:
    print("هوا خیلی گرمه! 🔥")
elif fahrenheit < 32:
    print("هوا خیلی سرده! ❄️")
else:
    print("هوا معتدله! 😊")
```

## 💡 نکات مهم

### 1. ترتیب مهمه!
```python
# ❌ غلط - قبل از تعریف استفاده کردیم
print(name)
name = "علی"

# ✅ درست - اول تعریف، بعد استفاده
name = "علی"
print(name)
```

### 2. فاصله‌گذاری
```python
# ✅ خوانا و تمیز
name = input("اسم: ")
age = int(input("سن: "))
print(f"سلام {name}!")

# ❌ شلوغ و نامرتب
name=input("اسم: ")
age=int(input("سن: "))
print(f"سلام {name}!")
```

### 3. نام‌گذاری معنادار
```python
# ❌ بد - نمی‌فهمیم چیه
a = 5
b = 3
c = a + b

# ✅ خوب - واضح و مشخص
price = 5
tax = 3
total = price + tax
```

## 🎨 الگوی استاندارد

این الگو رو برای همه برنامه‌هات استفاده کن:

```python
# ========================================
# عنوان برنامه
# توضیح کوتاه
# نویسنده: [اسم تو]
# ========================================

# 1. نمایش عنوان
print("=== عنوان برنامه ===")
print()

# 2. دریافت ورودی‌ها
# ورودی‌ها رو بگیر

# 3. انجام محاسبات
# محاسبات رو انجام بده

# 4. نمایش نتایج
# نتایج رو نشون بده

# 5. پیام پایانی
print("\nممنون که از برنامه استفاده کردی! 😊")
```

## 🎯 تمرین

یه برنامه بنویس که:
1. نام دو دوست رو بگیره
2. سن هر دو رو بگیره
3. بگه کدوم بزرگتره
4. بگه چند سال اختلاف سنی دارن

**راهنمایی:**
```python
# عنوان
print("=== مقایسه سن دوستان ===")

# ورودی
friend1 = input("اسم دوست اول: ")
age1 = int(input("سن دوست اول: "))

# ادامه بده...
```

## 🌟 نکته طلایی

یه برنامه خوب:
- ✅ ساده و قابل فهمه
- ✅ کامنت داره
- ✅ نام متغیرها معنادارن
- ✅ مرتب و تمیزه
- ✅ کار می‌کنه! 😄

## ➡️ مرحله بعد

حالا که با ساختار برنامه آشنا شدی، بیا بیشتر با ورودی و خروجی کار کنیم!
برو به [ورودی و خروجی](02_input_output.md)
