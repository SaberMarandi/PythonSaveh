# دستور if - تصمیم‌گیری در برنامه 🤔

## تصمیم‌گیری چیه؟

هر روز تو زندگی تصمیم می‌گیری:
- اگه هوا بارونی بود، چتر ببر ☔
- اگه گرسنه بودی، غذا بخور 🍕
- اگه خسته بودی، استراحت کن 😴

برنامه‌ها هم باید تصمیم بگیرن! 🤖

## ساختار if

```python
if شرط:
    # اگه شرط درست بود، این کارها رو انجام بده
    کد
```

⚠️ **مهم**: بعد از `if` باید `:` بذاری و خط بعدی رو با 4 فاصله شروع کنی!

## مثال‌های ساده

### مثال 1: بررسی سن
```python
age = 15

if age >= 13:
    print("تو نوجوان هستی! 🎉")
```

### مثال 2: بررسی نمره
```python
score = 18

if score >= 10:
    print("قبول شدی! 🎊")
```

### مثال 3: بررسی رمز عبور
```python
password = input("رمز عبور: ")

if password == "1234":
    print("خوش آمدید! ✅")
```

## عملگرهای مقایسه

```python
==  # مساوی
!=  # نامساوی
>   # بزرگتر
<   # کوچکتر
>=  # بزرگتر مساوی
<=  # کوچکتر مساوی
```

### مثال‌ها:
```python
# بررسی عدد مثبت
num = 5
if num > 0:
    print("عدد مثبته! ➕")

# بررسی عدد زوج
num = 8
if num % 2 == 0:
    print("عدد زوجه! 2️⃣")

# بررسی نام
name = "علی"
if name == "علی":
    print("سلام علی! 👋")
```

## برنامه‌های تعاملی

### بازی حدس عدد ساده
```python
secret = 7
guess = int(input("یه عدد بین 1 تا 10 حدس بزن: "))

if guess == secret:
    print("آفرین! درست حدس زدی! 🎉")
```

### بررسی دسترسی
```python
age = int(input("چند سالته؟ "))

if age >= 18:
    print("می‌تونی رای بدی! 🗳️")

if age >= 16:
    print("می‌تونی گواهینامه بگیری! 🚗")

if age >= 13:
    print("می‌تونی اینستاگرام داشته باشی! 📱")
```

### سیستم امتیاز
```python
points = int(input("چند امتیاز داری؟ "))

if points >= 100:
    print("سطح طلایی! 🥇")

if points >= 50:
    print("سطح نقره‌ای! 🥈")

if points >= 10:
    print("سطح برنزی! 🥉")
```

## چند شرط با هم

### با and (و)
```python
age = 15
has_ticket = True

if age >= 13 and has_ticket:
    print("می‌تونی فیلم ببینی! 🎬")
```

### با or (یا)
```python
day = "شنبه"

if day == "جمعه" or day == "پنج‌شنبه":
    print("امروز تعطیله! 🎉")
```

### با not (نه)
```python
is_raining = False

if not is_raining:
    print("می‌تونی بیرون بری! ☀️")
```

## خطاهای رایج

### خطا 1: فراموش کردن :
```python
# ❌ غلط
if age > 10
    print("سلام")

# ✅ درست
if age > 10:
    print("سلام")
```

### خطا 2: فراموش کردن فاصله
```python
# ❌ غلط
if age > 10:
print("سلام")

# ✅ درست
if age > 10:
    print("سلام")
```

### خطا 3: استفاده از = به جای ==
```python
# ❌ غلط - این یعنی مقداردهی!
if age = 10:
    print("سلام")

# ✅ درست - این یعنی مقایسه!
if age == 10:
    print("سلام")
```

## تمرین‌های سریع

### تمرین 1
```python
# یه برنامه بنویس که بگه عدد مثبته یا نه
num = int(input("یه عدد بنویس: "))
# کدت رو اینجا بنویس
```

### تمرین 2
```python
# یه برنامه بنویس که بگه آیا می‌تونی فیلم +15 ببینی
age = int(input("چند سالته؟ "))
# کدت رو اینجا بنویس
```

### تمرین 3
```python
# یه برنامه بنویس که رمز عبور رو چک کنه
password = input("رمز عبور: ")
# کدت رو اینجا بنویس
```

## پروژه: سیستم ورود ساده

```python
print("🔐 سیستم ورود")
print("-" * 30)

username = input("نام کاربری: ")
password = input("رمز عبور: ")

if username == "admin" and password == "1234":
    print("\n✅ ورود موفق!")
    print("خوش آمدید مدیر!")
```

## نکات مهم

1. ✅ همیشه بعد از `if` از `:` استفاده کن
2. ✅ کدهای داخل `if` رو با 4 فاصله بنویس
3. ✅ برای مقایسه از `==` استفاده کن نه `=`
4. ✅ می‌تونی چند شرط رو با `and` و `or` ترکیب کنی

## ➡️ مرحله بعد

حالا که با `if` آشنا شدی، بیا یاد بگیریم وقتی شرط درست نبود چیکار کنیم!
برو به [if-else](02_if_else.md)
