# if-else - دو راهی! 🔀

## مشکل if تنها چیه؟

فرض کن می‌خوای بگی:
- اگه قبول شدی، تبریک!
- اگه قبول نشدی، چی؟ 🤔

با `if` تنها نمی‌تونیم این کار رو بکنیم!

## راه حل: if-else

```python
if شرط:
    # اگه شرط درست بود
    کد 1
else:
    # اگه شرط غلط بود
    کد 2
```

## مثال‌های ساده

### مثال 1: قبولی یا ردی
```python
score = int(input("نمره‌ات چنده؟ "))

if score >= 10:
    print("قبول شدی! 🎉")
else:
    print("متأسفانه رد شدی 😔")
```

### مثال 2: زوج یا فرد
```python
num = int(input("یه عدد بنویس: "))

if num % 2 == 0:
    print(f"{num} زوجه! 2️⃣")
else:
    print(f"{num} فرده! 1️⃣")
```

### مثال 3: بزرگسال یا کودک
```python
age = int(input("چند سالته؟ "))

if age >= 18:
    print("تو بزرگسال هستی! 👨")
else:
    print("تو هنوز کودک هستی! 👶")
```

## برنامه‌های جالب

### بازی سکه
```python
import random

print("🪙 بازی سکه!")
print("شیر یا خط؟")

user_choice = input("انتخاب تو (شیر/خط): ")
coin = random.choice(["شیر", "خط"])

print(f"سکه افتاد: {coin}")

if user_choice == coin:
    print("بردی! 🎉")
else:
    print("باختی! 😢")
```

### چراغ راهنمایی
```python
light = input("چراغ چه رنگیه؟ (قرمز/سبز): ")

if light == "سبز":
    print("برو! 🚗")
else:
    print("وایسا! 🛑")
```

### بررسی رمز عبور
```python
correct_password = "python123"
user_password = input("رمز عبور: ")

if user_password == correct_password:
    print("✅ ورود موفق!")
    print("خوش آمدید!")
else:
    print("❌ رمز عبور اشتباهه!")
    print("دوباره امتحان کن")
```

## مثال‌های پیشرفته‌تر

### ماشین حساب ساده
```python
num1 = float(input("عدد اول: "))
num2 = float(input("عدد دوم: "))
operation = input("عملیات (+/-): ")

if operation == "+":
    result = num1 + num2
    print(f"جواب: {result}")
else:
    result = num1 - num2
    print(f"جواب: {result}")
```

### بررسی دما
```python
temp = float(input("دما چنده؟ "))

if temp >= 30:
    print("🔥 هوا گرمه! آب زیاد بخور")
else:
    print("❄️ هوا سرده! لباس گرم بپوش")
```

### سیستم تخفیف
```python
price = float(input("قیمت کالا: "))
has_coupon = input("کد تخفیف داری؟ (بله/خیر): ")

if has_coupon == "بله":
    final_price = price * 0.8  # 20% تخفیف
    print(f"با تخفیف: {final_price} تومان 🎉")
else:
    print(f"بدون تخفیف: {price} تومان")
```

## if-else تو در تو

```python
age = int(input("چند سالته؟ "))

if age >= 18:
    has_license = input("گواهینامه داری؟ (بله/خیر): ")
    
    if has_license == "بله":
        print("می‌تونی رانندگی کنی! 🚗")
    else:
        print("باید گواهینامه بگیری!")
else:
    print("هنوز سنت کمه!")
```

## خطاهای رایج

### خطا 1: فراموش کردن else
```python
# ❌ ناقص
if score >= 10:
    print("قبول")
# اگه قبول نشد چی؟

# ✅ کامل
if score >= 10:
    print("قبول")
else:
    print("رد")
```

### خطا 2: فاصله‌گذاری اشتباه
```python
# ❌ غلط
if age > 18:
    print("بزرگسال")
    else:  # else نباید فاصله داشته باشه!
    print("کودک")

# ✅ درست
if age > 18:
    print("بزرگسال")
else:
    print("کودک")
```

## تمرین‌ها

### تمرین 1: مثبت یا منفی
```python
# یه برنامه بنویس که بگه عدد مثبته یا منفی
num = int(input("عدد: "))
# کدت رو بنویس
```

### تمرین 2: بلند یا کوتاه
```python
# یه برنامه بنویس که بگه قدت بلنده یا کوتاه
# (بالای 170 بلند، زیرش کوتاه)
height = int(input("قدت چنده؟ "))
# کدت رو بنویس
```

### تمرین 3: تعطیل یا کاری
```python
# یه برنامه بنویس که بگه امروز تعطیله یا نه
day = input("امروز چه روزیه؟ ")
# کدت رو بنویس
```

## پروژه: بازی حدس عدد

```python
import random

print("🎮 بازی حدس عدد")
print("من یه عدد بین 1 تا 10 فکر کردم")

secret = random.randint(1, 10)
guess = int(input("حدس بزن: "))

if guess == secret:
    print("🎉 آفرین! درست حدس زدی!")
    print(f"عدد من {secret} بود")
else:
    print("😢 اشتباه!")
    print(f"عدد من {secret} بود")
    
    if guess > secret:
        print("تو بیشتر حدس زدی")
    else:
        print("تو کمتر حدس زدی")
```

## نکات طلایی

1. ✅ `else` همیشه بعد از `if` میاد
2. ✅ `else` نیاز به شرط نداره
3. ✅ فقط یکی از دو بخش اجرا میشه
4. ✅ می‌تونی `if-else` تو در تو بنویسی

## ➡️ مرحله بعد

حالا که با `if-else` راحتی، بیا یاد بگیریم چطوری بیشتر از 2 حالت رو چک کنیم!
برو به [if-elif-else](03_if_elif_else.md)
