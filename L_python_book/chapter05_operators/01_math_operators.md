# عملگرهای ریاضی ➕➖✖️➗

## محاسبه با پایتون! 🧮

پایتون مثل یه ماشین حساب قدرتمنده! می‌تونی با اعداد کلی کار جالب انجام بدی.

## 🎯 عملگرهای اصلی

### 1. جمع (+)
```python
# جمع دو عدد
a = 5
b = 3
result = a + b
print(result)  # 8

# جمع چند عدد
total = 10 + 20 + 30 + 40
print(total)  # 100
```

### 2. تفریق (-)
```python
# تفریق
a = 10
b = 3
result = a - b
print(result)  # 7

# عدد منفی
negative = -5
print(negative)  # -5
```

### 3. ضرب (*)
```python
# ضرب دو عدد
a = 4
b = 5
result = a * b
print(result)  # 20

# تکرار رشته!
text = "سلام " * 3
print(text)  # سلام سلام سلام 
```

### 4. تقسیم (/)
```python
# تقسیم معمولی
a = 10
b = 3
result = a / b
print(result)  # 3.3333333333333335

# همیشه عدد اعشاری برمی‌گردونه
result = 10 / 2
print(result)  # 5.0
```

### 5. تقسیم صحیح (//)
```python
# فقط قسمت صحیح
a = 10
b = 3
result = a // b
print(result)  # 3

# مثال: تقسیم شیرینی
candies = 17
kids = 5
per_kid = candies // kids
print(f"هر بچه {per_kid} تا شیرینی می‌گیره")  # 3
```

### 6. باقیمانده (%)
```python
# باقیمانده تقسیم
a = 10
b = 3
remainder = a % b
print(remainder)  # 1

# مثال: شیرینی اضافی
candies = 17
kids = 5
extra = candies % kids
print(f"{extra} تا شیرینی اضافه می‌مونه")  # 2
```

### 7. توان (**)
```python
# به توان رساندن
a = 2
b = 3
result = a ** b  # 2 به توان 3
print(result)  # 8

# مثال‌های دیگه
square = 5 ** 2  # 25
cube = 3 ** 3    # 27
print(f"5 به توان 2 = {square}")
print(f"3 به توان 3 = {cube}")
```

## 🎮 مثال‌های کاربردی

### مثال 1: ماشین حساب ساده
```python
print("🧮 ماشین حساب ساده")
print("-" * 30)

num1 = float(input("عدد اول: "))
num2 = float(input("عدد دوم: "))

print(f"\n{num1} + {num2} = {num1 + num2}")
print(f"{num1} - {num2} = {num1 - num2}")
print(f"{num1} × {num2} = {num1 * num2}")
print(f"{num1} ÷ {num2} = {num1 / num2}")
```

### مثال 2: محاسبه مساحت
```python
print("📐 محاسبه مساحت مستطیل")

length = float(input("طول: "))
width = float(input("عرض: "))

area = length * width
perimeter = 2 * (length + width)

print(f"\nمساحت: {area}")
print(f"محیط: {perimeter}")
```

### مثال 3: تبدیل واحد
```python
print("💰 تبدیل تومان به دلار")

toman = float(input("مبلغ به تومان: "))
rate = 50000  # نرخ دلار

dollar = toman / rate
print(f"{toman} تومان = {dollar:.2f} دلار")
```

### مثال 4: محاسبه سن
```python
print("🎂 محاسبه سن به روز")

age_years = int(input("سن شما (سال): "))

age_months = age_years * 12
age_days = age_years * 365
age_hours = age_days * 24

print(f"\nشما تقریباً:")
print(f"  {age_months} ماه")
print(f"  {age_days} روز")
print(f"  {age_hours} ساعت زندگی کردی! 🎉")
```

## 💡 نکات مهم

### 1. تقسیم بر صفر ❌
```python
# این کار خطا می‌ده!
# result = 10 / 0  # ZeroDivisionError

# درست:
num = 10
divisor = 0
if divisor != 0:
    result = num / divisor
else:
    print("نمی‌شه بر صفر تقسیم کرد!")
```

### 2. اعداد اعشاری
```python
# دقت در محاسبات اعشاری
result = 0.1 + 0.2
print(result)  # 0.30000000000000004

# برای نمایش بهتر:
print(f"{result:.2f}")  # 0.30
```

### 3. ترکیب عملگرها
```python
# می‌تونی ترکیب کنی
result = 2 + 3 * 4  # اول ضرب، بعد جمع
print(result)  # 14

# با پرانتز اولویت رو عوض کن
result = (2 + 3) * 4  # اول جمع، بعد ضرب
print(result)  # 20
```

## 🎯 تمرین‌های عملی

### تمرین 1: فروشگاه
```python
# یه برنامه بنویس که:
# - قیمت کالا رو بگیره
# - تعداد رو بگیره
# - مالیات 9% اضافه کنه
# - قیمت نهایی رو نشون بده

price = float(input("قیمت کالا: "))
quantity = int(input("تعداد: "))

# ادامه بده...
```

### تمرین 2: تقسیم عادلانه
```python
# یه برنامه بنویس که:
# - تعداد شیرینی رو بگیره
# - تعداد بچه‌ها رو بگیره
# - بگه هر بچه چند تا می‌گیره
# - بگه چند تا اضافه می‌مونه

candies = int(input("تعداد شیرینی: "))
kids = int(input("تعداد بچه‌ها: "))

# ادامه بده...
```

### تمرین 3: محاسبه سرعت
```python
# یه برنامه بنویس که:
# - مسافت (کیلومتر) رو بگیره
# - زمان (ساعت) رو بگیره
# - سرعت متوسط رو حساب کنه

distance = float(input("مسافت (km): "))
time = float(input("زمان (hour): "))

# ادامه بده...
```

## 🌟 چالش: ماشین حساب پیشرفته

یه ماشین حساب بساز که:
1. دو عدد بگیره
2. نوع عملیات رو بپرسه (+، -، *، /)
3. جواب رو نشون بده
4. اگه تقسیم بر صفر بود، پیغام خطا بده

```python
print("🧮 ماشین حساب پیشرفته")
print("-" * 40)

num1 = float(input("عدد اول: "))
num2 = float(input("عدد دوم: "))
operation = input("عملیات (+، -، *، /): ")

# ادامه بده...
```

## 📊 جدول خلاصه

| عملگر | نام | مثال | نتیجه |
|-------|-----|------|-------|
| + | جمع | 5 + 3 | 8 |
| - | تفریق | 5 - 3 | 2 |
| * | ضرب | 5 * 3 | 15 |
| / | تقسیم | 5 / 2 | 2.5 |
| // | تقسیم صحیح | 5 // 2 | 2 |
| % | باقیمانده | 5 % 2 | 1 |
| ** | توان | 5 ** 2 | 25 |

## ➡️ مرحله بعد

حالا که با عملگرهای ریاضی آشنا شدی، بیا یاد بگیریم چطوری اعداد رو با هم مقایسه کنیم!
برو به [عملگرهای مقایسه](02_comparison.md)
