# اولویت عملگرها 🎯

## ترتیب انجام عملیات

وقتی چند عملگر تو یه خط داریم، پایتون کدوم رو اول انجام میده؟ مثل ریاضی، یه ترتیب خاصی داره!

## 📊 جدول اولویت (از بالا به پایین)

| اولویت | عملگر | نام | مثال |
|--------|-------|-----|------|
| 1 | () | پرانتز | (2 + 3) |
| 2 | ** | توان | 2 ** 3 |
| 3 | *, /, //, % | ضرب، تقسیم | 5 * 2 |
| 4 | +, - | جمع، تفریق | 5 + 3 |
| 5 | <, <=, >, >= | مقایسه | 5 > 3 |
| 6 | ==, != | برابری | 5 == 5 |
| 7 | not | نه | not True |
| 8 | and | و | True and False |
| 9 | or | یا | True or False |

## 🎮 مثال‌های عملی

### مثال 1: ریاضی ساده
```python
# بدون پرانتز
result = 2 + 3 * 4
print(result)  # 14 (اول ضرب، بعد جمع)

# با پرانتز
result = (2 + 3) * 4
print(result)  # 20 (اول جمع، بعد ضرب)
```

### مثال 2: توان و ضرب
```python
# توان اولویت بالاتری داره
result = 2 * 3 ** 2
print(result)  # 18 (اول 3^2=9، بعد 2*9=18)

# با پرانتز
result = (2 * 3) ** 2
print(result)  # 36 (اول 2*3=6، بعد 6^2=36)
```

### مثال 3: مقایسه و منطقی
```python
# اول مقایسه، بعد and
age = 25
result = age > 18 and age < 65
print(result)  # True

# واضح‌تر با پرانتز
result = (age > 18) and (age < 65)
print(result)  # True
```

### مثال 4: ترکیب پیچیده
```python
# بدون پرانتز - گیج‌کننده!
result = 10 + 5 * 2 - 3 ** 2 / 3
print(result)  # 17.0

# با پرانتز - واضح!
result = 10 + (5 * 2) - ((3 ** 2) / 3)
print(result)  # 17.0

# مرحله به مرحله:
# 1. 3 ** 2 = 9
# 2. 9 / 3 = 3.0
# 3. 5 * 2 = 10
# 4. 10 + 10 = 20
# 5. 20 - 3.0 = 17.0
```

## 💡 نکات مهم

### 1. پرانتز همیشه اولویت داره
```python
# پرانتز اولویت رو عوض می‌کنه
print(2 + 3 * 4)      # 14
print((2 + 3) * 4)    # 20
print(2 + (3 * 4))    # 14 (مثل حالت اول)
```

### 2. عملگرهای هم‌سطح
```python
# وقتی اولویت یکسانه، از چپ به راست
result = 10 - 5 - 2
print(result)  # 3 (اول 10-5=5، بعد 5-2=3)

result = 20 / 4 / 2
print(result)  # 2.5 (اول 20/4=5، بعد 5/2=2.5)
```

### 3. not قبل از and و or
```python
# not اول اجرا میشه
result = not False and True
print(result)  # True

# مثل این:
result = (not False) and True
print(result)  # True
```

### 4. and قبل از or
```python
# and اول اجرا میشه
result = True or False and False
print(result)  # True

# مثل این:
result = True or (False and False)
print(result)  # True

# نه این:
result = (True or False) and False
print(result)  # False
```

## 🎯 مثال‌های کاربردی

### مثال 1: محاسبه قیمت نهایی
```python
print("💰 محاسبه قیمت")

price = 100000
quantity = 3
tax_rate = 0.09
discount_rate = 0.15

# بدون پرانتز - ممکنه اشتباه باشه
# total = price * quantity - discount_rate + tax_rate

# با پرانتز - درست و واضح
subtotal = price * quantity
discount = subtotal * discount_rate
after_discount = subtotal - discount
tax = after_discount * tax_rate
total = after_discount + tax

print(f"قیمت کل: {subtotal}")
print(f"تخفیف: {discount}")
print(f"بعد از تخفیف: {after_discount}")
print(f"مالیات: {tax}")
print(f"قیمت نهایی: {total}")
```

### مثال 2: بررسی محدوده
```python
print("📊 بررسی نمره")

score = float(input("نمره: "))

# بدون پرانتز - کار می‌کنه ولی گیج‌کننده
is_passing = score >= 60 and score <= 100

# با پرانتز - واضح‌تر
is_passing = (score >= 60) and (score <= 100)

# یا بهتر:
is_passing = 60 <= score <= 100

if is_passing:
    print("✅ قبول شدی!")
else:
    print("❌ مردود شدی!")
```

### مثال 3: شرط پیچیده
```python
print("🎫 بررسی ورود")

age = int(input("سن: "))
has_ticket = input("بلیط داری؟ (yes/no): ") == "yes"
is_vip = input("VIP هستی؟ (yes/no): ") == "yes"

# بدون پرانتز - خیلی گیج‌کننده!
# can_enter = age >= 18 and has_ticket or is_vip and age >= 16

# با پرانتز - واضح و درست
can_enter = (age >= 18 and has_ticket) or (is_vip and age >= 16)

if can_enter:
    print("✅ می‌تونی وارد بشی!")
else:
    print("❌ نمی‌تونی وارد بشی!")
```

## 🎯 تمرین‌های عملی

### تمرین 1: پیش‌بینی نتیجه
```python
# بدون اجرا، نتیجه رو حدس بزن:

# 1
result = 5 + 3 * 2
# جواب: ؟

# 2
result = (5 + 3) * 2
# جواب: ؟

# 3
result = 10 - 2 ** 3
# جواب: ؟

# 4
result = True or False and False
# جواب: ؟

# 5
result = not True or False
# جواب: ؟

# حالا اجرا کن و ببین درست حدس زدی یا نه!
```

### تمرین 2: اصلاح کد
```python
# این کد اشتباه کار می‌کنه. با پرانتز درستش کن:

price = 100
discount = 20
tax = 9

# اشتباه:
total = price - discount / 100 * price + tax / 100 * price

# درست کن:
# total = ؟
```

### تمرین 3: محاسبه BMI
```python
# فرمول BMI: وزن / (قد * قد)
# قد باید به متر باشه

weight = float(input("وزن (کیلوگرم): "))
height_cm = float(input("قد (سانتیمتر): "))

# بدون پرانتز - اشتباه!
# bmi = weight / height_cm / 100 ** 2

# با پرانتز - درست
# bmi = ؟

# ادامه بده...
```

### تمرین 4: شرط ورود
```python
# یه شرط بنویس که بگه:
# می‌تونی وارد بشی اگه:
# - سن بالای 18 باشه و بلیط داشته باشی
# یا
# - VIP باشی (بدون توجه به سن)
# و
# - ممنوع‌الورود نباشی

age = int(input("سن: "))
has_ticket = input("بلیط؟ (yes/no): ") == "yes"
is_vip = input("VIP؟ (yes/no): ") == "yes"
is_banned = input("ممنوع‌الورود؟ (yes/no): ") == "yes"

# شرط رو بنویس:
# can_enter = ؟
```

## 🌟 چالش: ماشین حساب علمی

یه ماشین حساب بساز که:
1. یه عبارت ریاضی بگیره (مثل: "2 + 3 * 4")
2. جواب رو حساب کنه
3. مراحل محاسبه رو نشون بده

```python
print("🧮 ماشین حساب علمی")
print("-" * 40)

expression = input("عبارت ریاضی: ")

# راهنمایی: از eval() استفاده کن (با احتیاط!)
result = eval(expression)
print(f"نتیجه: {result}")

# چالش: مراحل رو هم نشون بده!
```

## 📚 خلاصه قوانین

### قانون 1: پرانتز اول
```python
(2 + 3) * 4  # اول پرانتز
```

### قانون 2: توان بعد
```python
2 * 3 ** 2  # اول توان
```

### قانون 3: ضرب و تقسیم
```python
10 + 5 * 2  # اول ضرب
```

### قانون 4: جمع و تفریق
```python
10 - 5 + 2  # از چپ به راست
```

### قانون 5: مقایسه
```python
5 > 3 and 2 < 4  # اول مقایسه
```

### قانون 6: not، and، or
```python
not False and True or False  # not، بعد and، بعد or
```

## 🎨 نکته طلایی

**همیشه از پرانتز استفاده کن!**

حتی اگه لازم نباشه، کدت واضح‌تر میشه:

```python
# ❌ کار می‌کنه ولی گیج‌کننده
result = 10 + 5 * 2 - 3

# ✅ واضح و خوانا
result = 10 + (5 * 2) - 3
```

## ➡️ مرحله بعد

حالا که با اولویت عملگرها آشنا شدی، وقتشه تمرین کنی!
برو به [تمرین‌ها](05_exercises.md)
