# عملگرهای مقایسه 🔍

## مقایسه اعداد و مقادیر

گاهی باید ببینیم یه چیز بزرگتره، کوچیکتره یا برابره با چیز دیگه. برای این کار از عملگرهای مقایسه استفاده می‌کنیم!

## 🎯 عملگرهای مقایسه

### 1. مساوی (==)
```python
# بررسی برابری
a = 5
b = 5
result = a == b
print(result)  # True

# مثال
age = 18
if age == 18:
    print("تو 18 سالته!")
```

### 2. نامساوی (!=)
```python
# بررسی نابرابری
a = 5
b = 3
result = a != b
print(result)  # True

# مثال
password = "1234"
if password != "admin":
    print("رمز اشتباهه!")
```

### 3. بزرگتر (>)
```python
# بزرگتر بودن
a = 10
b = 5
result = a > b
print(result)  # True

# مثال
score = 85
if score > 80:
    print("نمره عالیه! 🎉")
```

### 4. کوچکتر (<)
```python
# کوچکتر بودن
a = 3
b = 7
result = a < b
print(result)  # True

# مثال
temperature = 15
if temperature < 20:
    print("هوا سرده! 🥶")
```

### 5. بزرگتر مساوی (>=)
```python
# بزرگتر یا مساوی
a = 10
b = 10
result = a >= b
print(result)  # True

# مثال
age = 18
if age >= 18:
    print("می‌تونی رای بدی!")
```

### 6. کوچکتر مساوی (<=)
```python
# کوچکتر یا مساوی
a = 5
b = 10
result = a <= b
print(result)  # True

# مثال
speed = 60
if speed <= 60:
    print("سرعت مجازه ✅")
```

## 🎮 مثال‌های کاربردی

### مثال 1: بررسی سن
```python
print("🎂 بررسی سن")

age = int(input("سن شما: "))

if age < 13:
    print("تو یه کودکی! 👶")
elif age < 18:
    print("تو یه نوجوونی! 🧒")
elif age < 60:
    print("تو یه بزرگسالی! 👨")
else:
    print("تو یه سالمندی! 👴")
```

### مثال 2: مقایسه دو عدد
```python
print("🔢 مقایسه دو عدد")

num1 = float(input("عدد اول: "))
num2 = float(input("عدد دوم: "))

if num1 > num2:
    print(f"{num1} بزرگتر از {num2} است")
elif num1 < num2:
    print(f"{num1} کوچکتر از {num2} است")
else:
    print(f"{num1} برابر با {num2} است")
```

### مثال 3: سیستم نمره‌دهی
```python
print("📊 سیستم نمره‌دهی")

score = float(input("نمره شما (0-100): "))

if score >= 90:
    grade = "A - عالی"
elif score >= 80:
    grade = "B - خوب"
elif score >= 70:
    grade = "C - متوسط"
elif score >= 60:
    grade = "D - قبولی"
else:
    grade = "F - مردودی"

print(f"نمره شما: {grade}")
```

### مثال 4: بررسی رمز عبور
```python
print("🔐 بررسی رمز عبور")

correct_password = "python123"
user_password = input("رمز عبور را وارد کنید: ")

if user_password == correct_password:
    print("✅ ورود موفق!")
    print("خوش آمدید!")
else:
    print("❌ رمز عبور اشتباه است!")
```

## 💡 نکات مهم

### 1. تفاوت = و ==
```python
# = برای تخصیص (assignment)
x = 5  # x رو برابر 5 می‌کنیم

# == برای مقایسه (comparison)
result = x == 5  # بررسی می‌کنیم x برابر 5 هست یا نه
print(result)  # True
```

### 2. مقایسه رشته‌ها
```python
# رشته‌ها رو هم می‌شه مقایسه کرد
name1 = "علی"
name2 = "علی"
print(name1 == name2)  # True

# حساس به حروف بزرگ و کوچیک
text1 = "Python"
text2 = "python"
print(text1 == text2)  # False
```

### 3. مقایسه زنجیره‌ای
```python
# می‌تونی چند مقایسه رو ترکیب کنی
age = 25
result = 18 <= age <= 65
print(result)  # True

# مثال
score = 75
if 60 <= score < 80:
    print("نمره متوسطه")
```

## 🎯 تمرین‌های عملی

### تمرین 1: بزرگترین عدد
```python
# یه برنامه بنویس که:
# - سه عدد بگیره
# - بزرگترین رو پیدا کنه
# - نمایش بده

num1 = float(input("عدد اول: "))
num2 = float(input("عدد دوم: "))
num3 = float(input("عدد سوم: "))

# ادامه بده...
```

### تمرین 2: بررسی سال کبیسه
```python
# یه برنامه بنویس که:
# - یه سال بگیره
# - بگه کبیسه یا نه
# قانون: سال باید بر 4 بخش‌پذیر باشه

year = int(input("سال: "))

# ادامه بده...
```

### تمرین 3: محاسبه تخفیف
```python
# یه برنامه بنویس که:
# - مبلغ خرید رو بگیره
# - اگه بالای 100 هزار بود، 10% تخفیف بده
# - اگه بالای 500 هزار بود، 20% تخفیف بده
# - قیمت نهایی رو نشون بده

amount = float(input("مبلغ خرید: "))

# ادامه بده...
```

### تمرین 4: بررسی دسترسی
```python
# یه برنامه بنویس که:
# - سن و کد ملی رو بگیره
# - اگه سن >= 18 و کد ملی درست باشه، دسترسی بده
# - وگرنه دسترسی نده

age = int(input("سن: "))
national_id = input("کد ملی: ")

# ادامه بده...
```

## 🌟 چالش: بازی حدس عدد

یه بازی بساز که:
1. یه عدد بین 1 تا 100 انتخاب کنه
2. از کاربر بخواد حدس بزنه
3. بگه حدس بزرگتره یا کوچکتره
4. تا وقتی که درست حدس نزده، ادامه بده

```python
import random

print("🎮 بازی حدس عدد")
print("-" * 40)

secret_number = random.randint(1, 100)
attempts = 0

print("یه عدد بین 1 تا 100 انتخاب کردم!")
print("حدس بزن چیه؟")

# ادامه بده...
```

## 📊 جدول خلاصه

| عملگر | معنی | مثال | نتیجه |
|-------|------|------|-------|
| == | مساوی | 5 == 5 | True |
| != | نامساوی | 5 != 3 | True |
| > | بزرگتر | 5 > 3 | True |
| < | کوچکتر | 3 < 5 | True |
| >= | بزرگتر مساوی | 5 >= 5 | True |
| <= | کوچکتر مساوی | 3 <= 5 | True |

## 🎨 نکته طلایی

نتیجه عملگرهای مقایسه همیشه `True` یا `False` است!

```python
result = 10 > 5
print(type(result))  # <class 'bool'>
```

## ➡️ مرحله بعد

حالا که با مقایسه آشنا شدی، بیا یاد بگیریم چطوری شرط‌های پیچیده‌تر بسازیم!
برو به [عملگرهای منطقی](03_logical.md)
